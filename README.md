# LSTM-SCADA-ANOMALY
> **Category:** SCADA Anomaly Detection  

## Description
Long Short-Term Memory network for multi-sensor SCADA normal behavior modeling and early failure detection.

## Structure
```
├── model.py           # Core neural network architecture / training script
├── requirements.txt   # Dependencies
├── .gitignore         # Environment and data exclusions
└── README.md          # Project overview
```

## Setup & Execution
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the model:
   ```bash
   python model.py
   ```

## Model Info
- **Repo name:** `wt-pm-lstm-scada-anomaly`
- **Category:** SCADA Anomaly Detection
- **Dependencies:** `tensorflow pandas numpy scikit-learn matplotlib`

> Scaffold generated locally for inspection — no GitHub API call made.
