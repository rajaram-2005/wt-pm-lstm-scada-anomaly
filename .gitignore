# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Generated artifacts (model registry, runs, figures, demo data).
# These are reproducible from `wtpm demo` and must not be committed.
artifacts/
runs/
reports/*.json
reports/*.md
!reports/.gitkeep
data/*.csv
data/*.npz
*.log

# unified platform: external research repos are cloned, never vendored
external/
runs_platform/
edge_artifacts/
production_result.json
scada_in/
scada_out/
scada_done/
scada_writeback.json
scada_demo_in.csv
hot_sample.csv
